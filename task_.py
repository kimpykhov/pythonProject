import random as r

def magic_sphere():
    options = ['refactor_your_code', 'take a look inside', 'to_fix_or_not_to_fix',  'git_pull_origin',
               'walk around of house', 'refactor_your_code', 'play with a cat', 'take a beer', 'listen a music']
    print(r.choice(options))
magic_sphere()
